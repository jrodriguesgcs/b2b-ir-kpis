# HubSpot Service Key (or private app access token as a drop-in fallback).
# Settings -> Integrations -> Service Keys (or Development -> Keys -> Service Keys).
# Required scopes: crm.objects.contacts.read, crm.objects.deals.read,
# crm.objects.meetings.read, crm.objects.owners.read,
# crm.schemas.contacts.read, crm.schemas.deals.read
HUBSPOT_ACCESS_TOKEN=
